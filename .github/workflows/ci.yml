name: CI

on:
  push:
    branches: [main, "redesign/**"]
  pull_request:

# The GPUI app links Metal and macOS APIs, so the build runs on macOS.
# Windows/Linux support is out of scope until the macOS app ships (INTENT.md D5).
jobs:
  check:
    name: build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      # Zero warnings from `cargo build` is a stated project invariant
      # (AGENT.md → Verification). Tests are no longer run in CI; run them
      # locally with `cargo test --workspace`.
      - name: Build
        run: cargo build --workspace

      # Informational: the repo carries pre-existing clippy lints, so this
      # step reports rather than gates.
      - name: Clippy
        run: cargo clippy --workspace --all-targets
